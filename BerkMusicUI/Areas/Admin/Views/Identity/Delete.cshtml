@model Identity
@{
    ViewData["Title"] = "Kimlik Bilgilerini Sil";
    Layout = "~/Areas/Admin/Views/Shared/_AdminLayout.cshtml";
}

<div class="card">
    <div class="card-body">
        <form asp-action="Delete" asp-controller="Identity" method="post">
            <p>
                @Model.Name isimli Kimlik Bilgisini silmek istediğinizden emin misiniz?
            </p>
            <button class="btn btn-danger">Evet</button>
            <a asp-action="Index" asp-controller="Identity" class="btn">İptal</a>
        </form>
    </div>
</div>