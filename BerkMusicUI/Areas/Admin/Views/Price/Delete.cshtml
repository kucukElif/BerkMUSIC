@model Price
@{
    ViewData["Title"] = "Fiyat Açıklaması Sil";
    Layout = "~/Areas/Admin/Views/Shared/_AdminLayout.cshtml";
}

<div class="card">
    <div class="card-body">
        <form asp-action="Delete" asp-controller="Price" method="post">
            <p>
                @Model.Title isimli fiyat bölümünü silmek istediğinizden emin misiniz?
            </p>
            <button class="btn btn-danger">Evet</button>
            <a asp-action="Index" asp-controller="Price" class="btn">İptal</a>
        </form>
    </div>
</div>
