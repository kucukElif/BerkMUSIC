@model Referance
@{
    ViewData["Title"] = "Referansi Güncelle";
    Layout = "~/Areas/Admin/Views/Shared/_AdminLayout.cshtml";
}
<div class="card">

    <div class="card-body">
        <div class="row">
            <div class="col-md-6">
                <!-- general form elements -->
                <div class="card card-primary">

                    <!-- /.box-header -->
                    <!-- form start -->
                    <div class="container">
                        <div class="row">
                            @if (Model.Photo == "noimage.jpg")
                            {
                                <img class="img-fluid" style="width:250px; height:300px;" src="~/images/noimage.jpg" />
                            }
                            else
                            {
                                <img class="img-fluid" style="width:250px; height:300px;" src="~/images/@Model.Photo" />

                            }
                        </div>
                    </div>
                    <form role="form" asp-action="Edit" asp-controller="Referance" method="post" enctype="multipart/form-data">
                        <div class="card-body">
                            <div class="form-group">
                                <label>Başlık</label>
                                <input asp-for="Text" type="text" class="form-control" placeholder="Metin Giriniz">
                            </div>
                            <div class="form-group">
                                <label>Link(URL)</label>
                                <input asp-for="Link" type="text" class="form-control" placeholder="URL Giriniz">
                            </div>


                            <div class="form-group">
                                <label>Resim Ekle</label>
                                <div class="input-group">
                                    <div class="custom-file">
                                        <input asp-for="Photo" type="file" class="custom-file-input" name="image" />
                                        <label class="custom-file-label">@Model.Photo</label>
                                    </div>
                                </div>
                            </div>
                        </div>

                        <!-- /.box-body -->

                        <div class="card-footer">
                            <button type="submit" class="btn btn-primary">Güncelle</button>
                        </div>
                    </form>
                </div>
                <!-- /.box -->
                <!-- Form Element sizes -->


            </div>
        </div>
    </div>
</div>
@section scripts{
    <script src="~/plugins/bs-custom-file-input/bs-custom-file-input.min.js"></script>
    <script>
    $(document).ready(function () {
        bsCustomFileInput.init();
    });
    </script>
  

}

